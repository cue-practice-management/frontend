import { FormatPracticeProcessStatusPipe } from './format-practice-process-status.pipe';

describe('FormatPracticeProcessStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatPracticeProcessStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
